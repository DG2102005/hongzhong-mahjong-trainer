# 红中麻将题库训练工具 — 操作说明

> 版本:v1.1 · 更新日期:2026-08-12
> 项目路径:`D:\TRAE\红中麻将助手\redcenter`
> 代码仓库:https://github.com/DG2102005/-

---

## 目录

- [一、环境准备](#一环境准备)
- [二、启动开发服务器](#二启动开发服务器)
- [三、使用工具](#三使用工具)
- [四、构建生产版本](#四构建生产版本)
- [五、运行测试](#五运行测试)
- [六、常见问题](#六常见问题)
- [七、停止服务](#七停止服务)
- [八、项目结构](#八项目结构)
- [九、版本管理(git 提交)](#九版本管理git-提交)

---

## 一、环境准备

### 1.1 安装 Node.js

访问 [https://nodejs.org/](https://nodejs.org/) 下载并安装 **LTS 版本**(建议 18.x 或 20.x)。

安装完成后,打开 **PowerShell** 验证安装:

```powershell
node --version
npm --version
```

若两条命令均能输出版本号,则环境就绪。

### 1.2 安装项目依赖

打开 PowerShell,进入项目目录:

```powershell
cd "D:\TRAE\红中麻将助手\redcenter"
npm install
```

> 项目默认已附带 `node_modules`,若未修改 `package.json` 可跳过此步。
> 若依赖缺失或版本更新,需重新执行此命令。

---

## 二、启动开发服务器

### 方式 1:命令行启动(推荐)

```powershell
cd "D:\TRAE\红中麻将助手\redcenter"
npm run dev
```

启动成功后,终端会显示:

```
  VITE v5.4.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: ...
```

浏览器会**自动打开** [http://localhost:5173](http://localhost:5173)(因 `vite.config.ts` 中 `open: true`)。

### 方式 2:直接用 npx

```powershell
cd "D:\TRAE\红中麻将助手\redcenter"
npx vite
```

### 方式 3:端口被占用时

若 5173 端口被占用,Vite 会自动切换到 5174 / 5175 等。
也可以手动指定端口:

```powershell
npx vite --port 3000
```

### 方式 4:通过 TRAE IDE 启动

在 TRAE 中打开本项目,在集成终端中执行 `npm run dev` 即可。
TRAE 会自动识别 Vite 服务并提示打开预览。

---

## 三、使用工具

### 3.1 界面说明

当前版本为**单一对弈界面**(与 3 个 AI 对战红中推倒胡,1 副牌,仅自摸胡)。

> 注:题库训练、选牌推演等模块的代码保留在 `src/quiz/`,当前界面未接入,后续版本将恢复。

### 3.2 对弈模式

1. 点击右上角"开始游戏"按钮发牌
2. 轮到自己出牌时,点击手牌中的任意一张即可打出(再次点击确认)
3. 出现碰/杠/胡选项时,按钮会悬浮显示在手牌上方,选择或点"放弃"
4. 一局结束后:中央牌桌翻开各家手牌,侧栏信息卡片下方展示**剩余牌墙**(核对要的牌还剩几张)
5. 点击"开始新一局"继续

**侧栏标签**:仅"信息"一栏,显示局数/庄家/当前/阶段/剩余牌数。

### 3.3 题库训练

> 注:该模块代码保留在 `src/quiz/`,当前界面未接入,以下为功能说明备用。

#### 入口
点击顶部"题库训练"按钮进入主页。

#### 6 种练习模式

| 模式 | 说明 |
|---|---|
| 📖 顺序练习 | 从 q001 依次练习全部 100 题 |
| 🏷️ 分类练习 | 按 6 大类别筛选(搭子取舍/听牌选择/红中运用/对子处理/金张判断/综合复杂) |
| 🎯 难度练习 | 按 easy / medium / hard 筛选 |
| 🎲 随机练习 | 乱序抽取 30 题 |
| ❌ 错题练习 | 仅显示历史答错的题 |
| ⭐ 收藏夹练习 | 仅显示已收藏的题 |

每张模式卡片显示该模式的**总题数 / 已答题数 / 正确率**。

#### 作答流程

1. 顶部显示"第 N / M 关"与 ⭐ 收藏按钮
2. 中部显示 **14 张手牌**(已自动理牌)
3. 问题:"红中麻将中打哪一张牌胡牌最快?"
4. 点击 **A / B / C / D** 4 张选项牌之一作答
5. 弹窗显示结果:
   - ✅ 正确:绿色对勾 + 解析
   - ❌ 错误:红色叉号 + 正确答案 + 解析
6. 关闭弹窗后,正确答案高亮绿色,错误选择高亮红色
7. 底部工具栏:
   - 进度统计(答对 X / Y)
   - 🧭 分析(查看该题的场景分析)
   - 上一题 / 下一题

#### 快捷键

| 按键 | 功能 |
|---|---|
| `1` `2` `3` `4` | 选择 A / B / C / D |
| `Enter` | 下一题 |
| `←` `→` | 上一题 / 下一题 |
| `Esc` | 关闭弹窗 |
| `F` | 收藏 / 取消收藏 |

#### 自定义题库

1. 在题库主页点击"📝 自定义题库"入口
2. 点击右上角"➕ 新建题目"
3. 在编辑器中依次填写:
   - 类别(6 类 + 自定义)
   - 难度(easy / medium / hard)
   - 手牌构造器(点击牌池添加,凑满 14 张)
   - 副露数(0-4)
   - 问题文字
   - 4 个选项(自动生成 / 手动选择)
   - 正确答案(A/B/C/D)
   - 解析文字
4. 点击"✅ 校验"检查题目合法性
5. 点击"👁 预览"试答
6. 点击"💾 保存"入库

**编辑器辅助按钮**:
- 🎲 自动填充随机:随机生成 14 张合法手牌
- 🤖 自动生成选项:基于引擎分析取前 4 弃张
- 🎯 自动判定答案:取评分最高的选项为正确答案
- 📝 自动生成解析:根据分析结果生成解析文字

### 3.4 选牌推演

> 注:该模块代码保留在 `src/quiz/`,当前界面未接入,以下为功能说明备用。

1. 点击顶部"选牌推演"按钮
2. 点击**牌池**(4 行 34 种牌,每种初始 4 张)选择牌加入手牌
3. 凑满 **14 张**后,底部"🧭 分析"按钮变为可点击
4. 点击"🧭 分析"显示场景分析卡片:
   - 当前向听数
   - 推荐弃牌
   - 各弃牌方案的进张门数与张数
   - 危险度评估
5. 底部工具栏:
   - ✕ 重置:清空所有手牌
   - ↶ 撤回:撤销最近一次操作
   - ↕ 关闭分析:隐藏分析卡片
   - 🧭 分析:显示场景分析
   - 🎲 随机摸牌:手牌 <14 时补齐到 14;=14 时随机替换一张

> 手牌会自动保存到 localStorage,刷新页面后不丢失。

---

## 四、构建生产版本

### 4.1 构建

```powershell
cd "D:\TRAE\红中麻将助手\redcenter"
npm run build
```

构建产物位于 `dist/` 目录,可直接部署到任意静态服务器。

### 4.2 预览构建结果

```powershell
npm run preview
```

默认预览地址:[http://localhost:4173](http://localhost:4173)

### 4.3 部署

将 `dist/` 目录上传至:
- Nginx / Apache 静态服务器
- Vercel / Netlify / Cloudflare Pages
- GitHub Pages

无需后端服务,所有数据存储在浏览器 localStorage。

---

## 五、运行测试

### 5.1 TypeScript 类型检查

```powershell
cd "D:\TRAE\红中麻将助手\redcenter"
npx tsc --noEmit
```

预期:**零错误**输出。

### 5.2 单元测试

```powershell
npx vitest run
```

预期输出:

```
Test Files  2 passed (2)
     Tests  46 passed (46)
```

测试覆盖:
- `src/game/__tests__/win.test.ts` — 胡牌判定(37 项)
- `src/quiz/__tests__/builtinQuestions.test.ts` — 内置 100 题校验(9 项)

### 5.3 监听模式(开发时)

```powershell
npx vitest
```

修改代码后自动重新运行测试。

---

## 六、常见问题

### 6.1 `npx` 命令找不到

**原因**:Node.js 安装时未将 npm 全局目录加入 PATH。

**解决方案**:
- 使用完整路径:`node "C:\Program Files\nodejs\node_modules\npx\bin\npx-cli.js" <命令>`
- 或重新安装 Node.js 并勾选"Add to PATH"
- 或手动将 `C:\Program Files\nodejs\` 加入系统环境变量 PATH

### 6.2 端口被占用

**现象**:启动时报错 `Port 5173 is in use`。

**解决方案**:
```powershell
# 方式 1:指定其他端口
npx vite --port 3000

# 方式 2:查找并释放端口
netstat -ano | findstr :5173
taskkill /PID <进程ID> /F
```

### 6.3 页面空白

**原因**:通常是浏览器缓存或 JavaScript 运行时错误。

**解决方案**:
1. 按 `F12` 打开开发者工具,查看 Console 错误信息
2. 按 `Ctrl + F5` 强制刷新(忽略缓存)
3. 清除浏览器缓存后重试
4. 若仍有错误,检查 `src/` 下代码是否完整

### 6.4 牌面图片不显示

**原因**:`public/tiles/` 目录缺少图片资源。

**解决方案**:
- 确认 `public/tiles/` 目录下有 34 张 PNG 文件
- 文件名规范见 `src/game/tileAssets.ts`
- 资源来源:`D:\美术素材\麻将3`

### 6.5 学习进度丢失

**原因**:浏览器禁用了 localStorage(隐私模式 / 无痕模式)。

**解决方案**:
- 使用正常浏览器模式(非隐私模式)
- 检查浏览器设置,确保允许站点使用本地存储
- 不要在清理浏览数据时勾选"网站数据"

### 6.6 修改代码不生效

**原因**:Vite 热更新偶发失败。

**解决方案**:
1. 保存所有文件
2. 在运行 Vite 的终端按 `Ctrl + C` 停止
3. 重新执行 `npm run dev`

### 6.7 自定义题目无法保存

**原因**:校验未通过(手牌不是 14 张、选项重复等)。

**解决方案**:
- 点击"✅ 校验"按钮查看具体错误
- 按错误提示修正后重新保存

---

## 七、停止服务

在运行 Vite 的 PowerShell 窗口中:

1. 按 `Ctrl + C`
2. 按 `Y` 确认终止

服务停止后,浏览器页面将无法访问。

---

## 八、项目结构

```
redcenter/
├── public/
│   └── tiles/              # 牌面图片资源(34 张 PNG)
├── src/
│   ├── components/         # 通用组件(Tile, HandRow, AdvisorPanel 等)
│   ├── game/               # 游戏核心逻辑
│   │   ├── types.ts        # 类型定义
│   │   ├── advisor.ts      # 辅助决策引擎
│   │   ├── win.ts          # 胡牌判定
│   │   ├── sort.ts         # 理牌算法
│   │   ├── quizTypes.ts    # 题库类型定义
│   │   └── __tests__/      # 单元测试
│   ├── quiz/               # 题库训练模块
│   │   ├── builtinQuestions.ts  # 100 道内置题
│   │   ├── storage.ts      # localStorage 存取
│   │   ├── QuizHome.tsx    # 题库主页
│   │   ├── QuizPlay.tsx    # 作答页
│   │   ├── QuizCategorySelect.tsx  # 分类/难度选择
│   │   ├── CustomList.tsx  # 自定义题库列表
│   │   ├── CustomEditor.tsx # 自定义题目编辑器
│   │   ├── Simulator.tsx   # 选牌推演器
│   │   ├── quiz.css        # 题库样式
│   │   └── __tests__/      # 题库测试
│   ├── hooks/              # React Hooks
│   ├── App.tsx             # 根组件
│   ├── main.tsx            # 入口文件
│   └── index.css           # 全局样式
├── index.html              # HTML 模板
├── package.json            # 依赖与脚本
├── tsconfig.json           # TypeScript 配置
├── vite.config.ts          # Vite 配置
└── 操作说明.md             # 本文档
```

---

## 附录:技术栈

| 技术 | 版本 | 用途 |
|---|---|---|
| React | 18.x | UI 框架 |
| TypeScript | 5.x | 类型系统 |
| Vite | 5.x | 构建工具与开发服务器 |
| Vitest | 1.x | 单元测试 |

---

## 九、版本管理(git 提交)

### 9.1 三句话理解

把电脑上的改动"拍照存档"(commit),再"上传相册"(push)到 GitHub。**本地改代码永远不影响 GitHub,只有执行 git 提交命令才会上传。**

### 9.2 三条核心命令

| 命令 | 作用 | 类比 |
|---|---|---|
| `git add .` | 把所有改动过的文件放入待提交区 | 挑好要洗的衣服放进篮子 |
| `git commit -m "说明"` | 打包成一版存档,带上说明文字 | 洗衣机洗好这一篮子 |
| `git push` | 把这一版上传到 GitHub | 晾到网上给大家看 |

### 9.3 什么时候用

**在代码改完、并且跑过验证之后**,想把这一版保存/分享时用。典型流程:

```powershell
cd "D:\TRAE\红中麻将助手\redcenter"
npm run dev        # 1. 先玩一下,确认功能正常
git add .          # 2. 确认没问题了,暂存所有改动
git commit -m "局终显示剩余牌墙, 修复碰杠布局跳动"   # 3. 写清楚这版改了什么
git push           # 4. 上传到 GitHub
```

- commit 说明写**这版改了什么**(如上面示例),不要写"修改"这种废话
- 改了好几个功能,想拆成几版提交:先 `git commit -m "第一版说明"`,再改回部分文件后 `git add` 单独提交(新手可忽略,先养成一段一提交的习惯)

### 9.4 提交前常用辅助命令

```powershell
git status        # 查看哪些文件被改了(红=未暂存, 绿=已暂存)
git diff          # 具体看改了什么内容
git log --oneline # 查看提交历史
```

### 9.5 注意事项

- `node_modules`、`dist` 已被 `.gitignore` 排除,`git add .` 不会误传,不用管
- 只有 `push` 之后 GitHub 上才看得到;`add`/`commit` 只在本地
- 如果只想提交某 1 个文件:`git add src/App.tsx`(`git add .` 是全选)
- 项目仓库:https://github.com/DG2102005/- (分支 main)

---

**联系方式**:如有问题,请查阅项目源码或联系开发者。
